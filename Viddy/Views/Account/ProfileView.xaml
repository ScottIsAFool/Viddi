<views:BasePage x:Class="Viddy.Views.Account.ProfileView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:views="using:Viddy.Views"
                xmlns:controls="using:Viddy.Controls"
                xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
                xmlns:behaviours="using:Viddy.Behaviours"
                xmlns:core="using:Microsoft.Xaml.Interactions.Core"
                mc:Ignorable="d"
                Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
                DataContext="{Binding Profile, Source={StaticResource Locator}}">
    <interactivity:Interaction.Behaviors>
        <behaviours:StatusBarBehavior IsVisible="False" />
        <core:EventTriggerBehavior EventName="Loaded">
            <core:InvokeCommandAction Command="{Binding PageLoadedCommand}" />
        </core:EventTriggerBehavior>
    </interactivity:Interaction.Behaviors>

    <views:BasePage.BottomAppBar>
        <CommandBar>
            <CommandBar.PrimaryCommands>
                <AppBarButton Label="refresh"
                              Command="{Binding RefreshCommand}">
                    <AppBarButton.Icon>
                        <SymbolIcon Symbol="Refresh" />
                    </AppBarButton.Icon>
                </AppBarButton>
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </views:BasePage.BottomAppBar>

    <Grid>
        <controls:ProfileHeaderControl DataContext="{Binding User}"/>
        <controls:InitialisingListView ItemsSource="{Binding Videos}"
                                       ItemTemplate="{StaticResource VideoTemplate}"
                                       IsEmpty="{Binding IsEmpty}"
                                       IsInitialising="{Binding ProgressIsVisible}"
                                       Header="{Binding}">
            <controls:InitialisingListView.HeaderTemplate>
                <DataTemplate>
                   <controls:ProfileHeaderControl/>
                </DataTemplate>
            </controls:InitialisingListView.HeaderTemplate>
            <controls:InitialisingListView.FooterTemplate>
                <DataTemplate>
                    <Grid Height="47"
                          Visibility="{Binding CanLoadMore, Converter={StaticResource VisibilityConverter}}">
                        <controls:CustomHyperlinkButton Command="{Binding LoadMoreCommand}"
                                                        Content="load more"
                                                        HorizontalContentAlignment="Center"
                                                        HorizontalAlignment="Center"
                                                        VerticalAlignment="Top"
                                                        Visibility="{Binding IsLoadingMore, Converter={StaticResource InverseVisibilityConverter}}"/>
                        <ProgressBar HorizontalAlignment="Center"
                                     Width="130"
                                     IsIndeterminate="{Binding IsLoadingMore}"
                                     Visibility="{Binding IsLoadingMore, Converter={StaticResource VisibilityConverter}}"/>
                    </Grid>
                </DataTemplate>
            </controls:InitialisingListView.FooterTemplate>
        </controls:InitialisingListView>

    </Grid>
</views:BasePage>
